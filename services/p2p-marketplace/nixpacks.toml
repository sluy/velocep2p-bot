[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npm run build", "cp -r dist /tmp/dist-backup"]

[start]
cmd = "cp -r /tmp/dist-backup dist && npx prisma db push --accept-data-loss && node dist/main.js"
